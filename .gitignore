# 1. Build artifacts (generated by Maven/Gradle)
# These are compiled outputs that can be regenerated from source
# Maven build directory
target/
!target/generated-sources/  # Keep auto-generated sources if needed

# Gradle build directories
build/
.gradle/
!gradle/wrapper/gradle-wrapper.jar  # Keep Gradle wrapper
!**/src/main/**/build/
!**/src/test/**/build/

# 2. Dependency libraries
# Avoid committing manually added jars - dependencies should be managed via build files
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar
lib/
!lib/*.jar  # Uncomment if you need to track specific manually managed jars

# 3. IDE configuration files
# These are user/environment specific and shouldn't be shared
# IntelliJ IDEA
.idea/
*.iws
*.iml
*.ipr
out/

# Eclipse
.classpath
.project
.settings/
bin/

# VS Code
.vscode/
*.code-workspace
.history/

# 4. System/temporary files
.DS_Store  # macOS file system metadata
Thumbs.db  # Windows thumbnail cache
*.swp      # Vim swap files
*.swo      # Vim swap files

# 5. Log files
# Logs are generated at runtime and shouldn't be in version control
logs/
*.log
*.log.*

# 6. Configuration files
# Exclude actual environment configs (may contain secrets/passwords)
src/main/resources/application-dev.yml
src/main/resources/application-local.yml
# Keep example config templates (commit these as documentation)
!src/main/resources/application-dev.example.yml
!src/main/resources/application-local.example.yml

# 7. Test related files
# Test reports and coverage data are generated at runtime
*.surefire.html
*.failsafe.html
*.jacoco/
coverage/

# 8. Other temporary/backup files
*.tmp
*.bak
*.orig
*.rej
.DS_Store
